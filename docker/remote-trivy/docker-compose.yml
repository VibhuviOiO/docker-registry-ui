services:
  registry:
    image: registry:2
    ports:
      - "5001:5000"
    environment:
      REGISTRY_STORAGE_DELETE_ENABLED: "true"
    volumes:
      - registry-data:/var/lib/registry
    networks:
      - registry-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:5000/v2/"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  trivy-server:
    image: aquasec/trivy:latest
    command: ["server", "--listen", "0.0.0.0:8080"]
    volumes:
      - trivy-server-data:/root/.cache/trivy
    networks:
      - registry-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  registry-ui:
    build:
      context: ../..
      dockerfile: Dockerfile
    image: docker-registry-ui:local
    ports:
      - "5000:5000"
    environment:
      LOG_LEVEL: INFO
      READ_ONLY: "false"
      REGISTRIES: |-
        [
          {
            "name": "Local Registry",
            "api": "http://registry:5000",
            "vulnerabilityScan": {
              "enabled": true,
              "scanner": "trivy",
              "scannerUrl": "http://trivy-server:8080"
            }
          }
        ]
    volumes:
      - ui-data:/app/data
    depends_on:
      registry:
        condition: service_healthy
      trivy-server:
        condition: service_healthy
    networks:
      - registry-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://127.0.0.1:5000/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

volumes:
  registry-data:
  ui-data:
  trivy-server-data:

networks:
  registry-net:
    driver: bridge
